% Facts: bird(Name).
bird(sparrow).
bird(eagle).
bird(penguin).
bird(ostrich).
bird(pigeon).

% Facts: cannot_fly(BirdName). These are exceptions.
cannot_fly(penguin).
cannot_fly(ostrich).

% Rule: A bird can fly if it is a bird and not in the cannot_fly list.
can_fly(Bird) :-
    bird(Bird),
    \+ cannot_fly(Bird). % \+ means 'not'

% To query this database:
% Can a sparrow fly?
% ?- can_fly(sparrow).
% Expected output: true.

% Can a penguin fly?
% ?- can_fly(penguin).
% Expected output: false.
